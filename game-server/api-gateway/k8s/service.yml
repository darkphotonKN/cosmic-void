apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: default
  labels:
    app: cosmic-void
    component: api-gateway
spec:
  type: ClusterIP
  selector:
    app: cosmic-void
    component: api-gateway
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP